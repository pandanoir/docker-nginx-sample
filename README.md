# Quick Start

```
$ ./start.sh
$ curl localhost:8080
```

# docker-compose を使ってコンテナを起動

```
$ docker-compose up
```

# Docker を使ってコンテナを起動

```
$ ./start.sh
```

# ネットワークを使用

```
$ COMPOSE_FILE=docker-compose.yml:docker-compose.with-network.yml docker-compose up
```
